name: Lint
on: [pull_request, push]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install node_modules
      run: yarn
    - name: Lint Node.js
      run:  yarn run lint